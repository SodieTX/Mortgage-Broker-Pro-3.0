# Database Configuration
POSTGRES_USER=mortgage_user
POSTGRES_PASSWORD=mortgage_pass
POSTGRES_DB=mortgage_broker_pro

# PgAdmin Configuration
PGADMIN_EMAIL=admin@mortgage.local
PGADMIN_PASSWORD=admin

# Application Configuration (for future use)
NODE_ENV=development
APP_PORT=3000
API_PREFIX=/api/v1

# Security (generate your own for production)
JWT_SECRET=your-secret-key-here
ENCRYPTION_KEY=your-encryption-key-here
